import { StyleSheet } from "react-native"

export default {}

export const PaginationFooterStyle = StyleSheet.create({
    PaginationFooterContainer:{
        flexDirection:'row',
        padding:8,
        justifyContent:'space-between'
    }
})